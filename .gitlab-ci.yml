image: elixir:latest

stages:
  - test
  - publish

before_script:
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.get

mix:test:
  stage: test
  script:
    - MIX_ENV=test mix all

