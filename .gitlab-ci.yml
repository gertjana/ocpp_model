image: elixir:latest

stages:
  - build
  - test
  - publish

before_script:
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.get

mix:test:
  stage: test
  script:
    - MIX_ENV=test mix test

mix:build:
  stage: build
  script:
   - MIV_ENV=prod mix all