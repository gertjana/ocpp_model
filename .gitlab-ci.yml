image: elixir:latest

stages:
  - test
  - lint
  - publish

before_script:
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.get

mix:test:
  stage: test
  script:
    - MIX_ENV=test mix coveralls.json --trace
  after_script:
    - bash <(curl -s https://codecov.io/bash)

mix:lint:
  stage: lint
  script:
    - MIX_ENV=test mix credo --strict

